name: NUnit.Engine.Api.CI

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read
  checks: write

env:
  DOTNET_NOLOGO: true                     # Disable the .NET logo
  DOTNET_CLI_TELEMETRY_OPTOUT: true       # Disable sending .NET CLI telemetry

jobs:
  ContinuousIntegration:
    name: Continuous Integration Windows Build
    runs-on: windows-latest

    env:
        MYGET_API_KEY: ${{ secrets.PUBLISH_MYGET_ORG }}
        NUGET_API_KEY: ${{ secrets.PUBLISH_NUGET_ORG }}
        CHOCO_API_KEY: ${{ secrets.PUBLISH_CHOCOLATEY_ORG }}
        GITHUB_ACCESS_TOKEN: ${{ secrets.GH_ACCESS_CP }}

    steps:
    - name: ⤵️ Checkout Source
      uses: actions/checkout@v7
      with:
        fetch-depth: 0

    - name: 🛠️ Setup .NET
      uses: actions/setup-dotnet@v6
      with:
        dotnet-version: |
            8.0.x
            9.0.x
            10.0.x
        
    - name: 🛠️ Install dotnet tools
      run: dotnet tool restore

    # For reasons I don't understand, the Cake script doesn't work in a workflow
    # unless they are restored before the script runs, which this command does.  
    #- name: 🛠️ List Packages
    #  run: dotnet list package --include-transitive

    - name: 🔨 Build, Test, Package and Publish (Cake script decides)
      #run: dotnet cake --target=ContinuousIntegration --configuration=Release #--verbosity=diagnostic
      uses: cake-build/cake-action@v3
      with:
        target: ContinuousIntegration
        configuration: Release
        #verbosity: diagnostic

    # Upload packages
    - name: 💾 Upload Packages
      if: always()
      uses: actions/upload-artifact@v7
      with:
        name: Packages
        path: packages

# Need to set work directory and give testresult files differing names
#    - name: 💾 Upload test results
#      uses: actions/upload-artifact@v7
#      with:
#        name: TestResults (Windows)
#        path: TestResults
#      if: ${{ always() }}

# Need to implement test-reporter in NUnit
#    - name: 🧪 Test Report
#      if: ${{ always() }}
#      uses: ./.github/actions/test-reporter
#      with:
#        name: "Tests (${{ runner.os }})"
#        path: TestResults/**/*.trx
#        fail-on-error: false
#        token: ${{ secrets.GITHUB_TOKEN }}
